# NOGADA legal site — working rules

- Static HTML (GitHub Pages), no build step. Match the existing page style (tokens on `:root`, dark/light auto, 640px column, phone-width first).
- Work in Claude Code cloud sessions (claude.ai/code); commit and push every finished change and open a PR — never leave work only on a local machine.
- User-facing pages that crew read should offer English, Korean and Simplified Chinese.
